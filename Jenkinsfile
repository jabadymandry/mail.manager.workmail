pipeline {
    agent any
    stages {
        stage('Check variable'){
            steps {
                echo "${DomainName}"
                echo "${InfoMailService}"

            }
        }
    }
}